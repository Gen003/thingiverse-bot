services:
  - type: web
    name: thingiverse-bot
    env: python
    buildCommand: ""
    startCommand: "python main.py"
    envVars:
      - key: BOT_TOKEN
        value: ${BOT_TOKEN}
      - key: CHAT_ID
        value: ${CHAT_ID}
